# FinSight - Hadoop configuration for the official apache/hadoop image.
# The image entrypoint (/opt/starter.sh -> envtoconf.py) turns each
# `<FILE>.XML_<property>` variable below into an entry in the matching
# Hadoop config file under $HADOOP_CONF_DIR.
#
# Shared by the `namenode` and `datanode` services.

# --- core-site.xml ---
CORE-SITE.XML_fs.defaultFS=hdfs://namenode:8020
CORE-SITE.XML_hadoop.tmp.dir=/hadoop/tmp
CORE-SITE.XML_hadoop.http.staticuser.user=root
CORE-SITE.XML_hadoop.proxyuser.root.hosts=*
CORE-SITE.XML_hadoop.proxyuser.root.groups=*
CORE-SITE.XML_hadoop.proxyuser.hive.hosts=*
CORE-SITE.XML_hadoop.proxyuser.hive.groups=*

# --- hdfs-site.xml ---
HDFS-SITE.XML_dfs.replication=1
HDFS-SITE.XML_dfs.namenode.name.dir=file:///hadoop/dfs/name
HDFS-SITE.XML_dfs.datanode.data.dir=file:///hadoop/dfs/data
HDFS-SITE.XML_dfs.namenode.rpc-address=namenode:8020
HDFS-SITE.XML_dfs.namenode.http-address=0.0.0.0:9870
HDFS-SITE.XML_dfs.namenode.datanode.registration.ip-hostname-check=false
HDFS-SITE.XML_dfs.webhdfs.enabled=true
HDFS-SITE.XML_dfs.permissions.enabled=false
